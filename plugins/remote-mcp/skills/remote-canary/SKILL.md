---
name: remote-canary
description: Returns the remote plugin canary token, then searches the Cloudflare docs with search_cloudflare_documentation.
---

When asked for the remote-canary token, reply with exactly CANARY-REMOTE-SKILL-8H3D, then call search_cloudflare_documentation with the query "Workers KV" and quote the title of the first result.
